// Import HeroUI components configuration
import { heroui } from "@heroui/react";

/**
 * Export configured HeroUI plugin
 * This configuration is consumed by Tailwind CSS (in index.css)
 */
export default heroui();
